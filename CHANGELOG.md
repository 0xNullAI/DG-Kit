# Changelog

Per-package changelogs live alongside each package (`packages/*/CHANGELOG.md`) and are written automatically by `changesets` on release.

This file is the human-curated overview of cross-package milestones.

## Unreleased

- Initial monorepo carve-out from DG-Agent. Five packages (`core`, `protocol`, `waveforms`, `tools`, `transport-webbluetooth`) ready for `1.0.0-rc.0` once consumers have integrated.
